{
  "name": "story-relay-official-demo",
  "version": "1.0.0",
  "description": "Story Relay — official demo. Prebuilt scripted walkthrough + build-your-own mode that generates the film with the Runway API. Visuals match the Figma file exactly (white + lime).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
